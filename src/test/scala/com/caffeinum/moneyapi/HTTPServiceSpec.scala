package com.caffeinum.moneyapi

import org.scalatest._
import com.caffeinum.moneyapi._



class HTTPServiceSpec extends FlatSpec with Matchers {

  "API" should "save user correctly" in {
    // val newUserPost = Input.post("/users").withForm("username" -> "admin")
    //
    // var newUserJson = createUser(newUserPost).awaitValueUnsafe()


  }
}
